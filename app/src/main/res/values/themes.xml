<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DeltaruneTextBoxApp" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- You can customize your theme here if needed. -->
        <!-- For example:
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        -->
    </style>

    <!-- NoActionBar variant used for fullscreen activities -->
    <style name="Theme.DeltaruneTextBoxApp.NoActionBar" parent="Theme.DeltaruneTextBoxApp">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>